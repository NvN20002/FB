{
  "name": "backend",
  "private": true,
  "version": "1.0.0",
  "main": "server.js", // Pointing to your server file
  "scripts": {
    "start": "node server.js", // Start command for production
    "dev": "nodemon server.js", // For development, use nodemon for auto-restart
    "build": "echo 'No build step for backend'" // Placeholder if no build step is required
  },
  "dependencies": {
    "express": "^4.18.2", // Example dependency
    "mongoose": "^6.7.0", // Example dependency for MongoDB
    "cors": "^2.8.5", // Middleware for CORS
    "dotenv": "^16.0.3" // To manage environment variables
  },
  "devDependencies": {
    "nodemon": "^2.0.22", // For development, automatically restarts the server
    "eslint": "^8.55.0" // Include ESLint for linting if desired
  }
}
